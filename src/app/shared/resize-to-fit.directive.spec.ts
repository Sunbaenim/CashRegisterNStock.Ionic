import { ResizeToFitDirective } from './resize-to-fit.directive';

describe('ResizeToFitDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeToFitDirective();
    expect(directive).toBeTruthy();
  });
});
